[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn -b 0.0.0.0:$PORT app:app --workers 2 --timeout 120 --access-logfile -"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
